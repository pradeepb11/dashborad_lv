import { CurrencyindPipe } from './currencyind.pipe';

describe('CurrencyindPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyindPipe();
    expect(pipe).toBeTruthy();
  });
});
